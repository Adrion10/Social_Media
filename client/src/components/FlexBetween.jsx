import { Box } from "@mui/material";
import { styled } from "@mui/system";

const FlexBetween = styled(Box)({
  display: "flex",
  justiyContenet: "space-between",
  alignItems: "center",
});

export default FlexBetween;
